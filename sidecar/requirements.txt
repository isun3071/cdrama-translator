# Session 2 (contract skeleton) — light, fast install.
fastapi
uvicorn          # plain, not uvicorn[standard]: uvloop/httptools lag new Pythons
pydantic>=2

# Session 2 next step (OCR) — heavy; added when we wire real reads. See README
# for the Python-version caveat (paddle wheels lag Python 3.14).
# paddleocr
# paddlepaddle
